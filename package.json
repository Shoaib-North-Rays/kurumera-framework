{
  "name": "kurumera-framework",
  "version": "0.1.0",
  "private": true,
  "description": "Kurumera Next.js theme framework — CLI, Storefront SDK, theme helpers, and the base theme.",
  "workspaces": [
    "packages/*",
    "base-theme"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^20.14.0"
  },
  "license": "UNLICENSED"
}
