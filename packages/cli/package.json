{
  "name": "@kurumera/cli",
  "version": "0.3.1",
  "description": "Kurumera CLI — login, scaffold, dev, check, push, preview, publish.",
  "type": "module",
  "bin": {
    "kurumera": "dist/bin.js"
  },
  "files": [
    "dist",
    "template"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json && node scripts/bundle-template.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "prepack": "npm run build"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  },
  "license": "UNLICENSED"
}
