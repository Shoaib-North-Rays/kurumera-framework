{
  "name": "@kurumera/cli",
  "version": "0.1.0",
  "description": "Kurumera CLI — login, scaffold, dev, check, push, preview, publish.",
  "type": "module",
  "bin": { "kurumera": "dist/bin.js" },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "license": "UNLICENSED"
}
