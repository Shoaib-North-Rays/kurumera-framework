{
  "name": "@kurumera/cli",
  "version": "0.1.15",
  "description": "Kurumera CLI — login, scaffold, dev, check, push, preview, publish.",
  "type": "module",
  "bin": {
    "kurumera": "dist/bin.js"
  },
  "files": [
    "dist",
    "template"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/bundle-template.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "prepack": "npm run build"
  },
  "license": "UNLICENSED"
}
